name: Trigger on New Tag

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: read

jobs:
  call_orchestrator:
    name: Call Orchestrator for Tag
    uses: ./.github/workflows/orchestrator.yaml
    with:
      trigger_type: 'tag'
      github_event: ${{ toJson(github.event) }}
